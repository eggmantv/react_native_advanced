import {
  StyleSheet
} from 'react-native';

const GlobalStyle = StyleSheet.create({
  btn: {
    color: 'white',
    fontSize: 20,
    fontWeight: "900",
    textAlign: 'center',
    paddingVertical: 15,
  }
})

module.exports = GlobalStyle;
